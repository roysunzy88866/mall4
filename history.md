# 后端需求共创 · 进程记录(history)

> 「需求共创」skill 的过程日志。决策结论在 [后端需求.md §9](后端需求.md);本文记「怎么一步步走到的」。

## 2026-06-03 · 后端需求共创开场
- 背景:商店4 第 4 轮起点。前端范围/UI 已在 `需求共识.md` 锁定,本轮只补后端。
- 已读:`需求共识.md`、`design_handoff_ridemall_carstore/`(README + data.js 等)、`CLAUDE.md`。
- 愿景清晰(产品已定),**跳过脑洞发散**;直接进后端边界扫描。
- **第 1 批边界(4 问,AskUserQuestion)**,用户全选推荐项:
  - D1 推荐位文案 = 自动套商品信息
  - D2 订单 = 整单快照
  - D3 车机订单历史 = 只存服务器(按设备号)
  - D4 支付落库 = 先落库再显示成功
- **第 2 批边界(2 问)**,用户全选推荐项:
  - D5 停用分类 = 整类在车机隐藏
  - D6 预置订单 = 预置 3~5 笔,车机空态起步
- **行业调研**:跳过(技术栈已锁的内部 demo 后端,无市面惯例可参考)。
- 产出:`后端需求.md` 草案(含数据模型、对外接口、后台操作、业务规则、seed、安全、错误语义、决策日志、待定项)。
- 我补的默认(待用户否决):D7 为你推荐按上架倒序、D8 删被订单引用的商品允许、D9 删有商品的分类拦截。
- 待用户定:① 本文档独立 vs 并入需求共识;② 真实商品图素材;③ 共识 §1「车机不存订单」措辞是否同步收紧。
- **下一步(用户既定顺序)**:用户确认后端需求 → 补项目文档骨架 → 写测试方案。

## 2026-06-03 · 后端需求确认 + 收尾决定
- 用户逐条确认后端需求草案:D1~D6 拍板,D7~D9 默认获认可。
- 收尾 3 问(均推荐):文档独立成 `后端需求.md`;共识 §1 措辞同步收紧(已改);商品图先占位起步。
- 已同步 `需求共识.md`:§1 车机存储措辞收紧 + 顶部加指针指向 `后端需求.md`。
- 后端需求 → ✅ 已确认。下一步补文档(架构重评估 + 接口清单 + README + ADR)→ 测试方案。

## 2026-06-03 · 补文档 + 测试方案(本轮收尾)
- 拍板:架构 = 务实分层(ADR-0001);补文档范围 = 先补测试方案要用的。
- 新建:`docs/architecture.md`、`docs/api.md`、`README.md`、`docs/adr/0001`(架构)、`docs/adr/0002`(§1 收紧)、`测试方案.md`(单元/集成/端到端)。
- CLAUDE.md:架构按务实分层写回 + 恢复架构硬规矩 + 状态/文档地图更新(140 行,守住 200 红线)。
- 建造流程类(story-map / user-stories / delivery-process / git 脚手架)按约定留到动工(C1)前补。
- 本轮 4 件事(域名 / 测试方案 / CLAUDE.md / 后端需求)全部完成。
