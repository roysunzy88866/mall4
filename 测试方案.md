# 测试方案 · RIDEMALL 车载商店

> 依据 [后端需求.md](后端需求.md)、[docs/architecture.md](docs/architecture.md)、[需求共识.md §5 验收标准](需求共识.md)。
> 三层:**单元 / 集成 / 端到端**。纪律:红绿循环(先写失败测试 → 转绿 → 重构),每写一个函数/接口立刻测(见 [CLAUDE.md](CLAUDE.md))。

## 0. 策略一句话

后端是**数据正确性**的关键 → 自动化火力集中在后端(单元 + 集成 + API 级端到端);车机端 UI → 关键逻辑单测 + 一页**手测清单**走真机/模拟器。理由:Compose UI + 模拟器做全自动化录制回放,对这个 demo 性价比低,用手测清单替代更划算(这是「测试方式」的取舍,记录在案)。

---

## 1. 单元测试(Unit)· 跑得最快、最多

**后端 `rules/`(纯函数)—— 强制 100% 覆盖**,逐条:

| 规则 | 要测什么(例) |
|---|---|
| 推荐位 3~5 | 2 个 → 报错;3/4/5 个 → 通过;6 个 → 报错 |
| 整单快照 | 下单后改/删商品,订单里的名/图/价**不变** |
| 价格快照来源 | 用车机上送的锁定价,**不**用商品现价 |
| Banner 文案自动套 | 标题=商品名、价=商品价、描述=描述节选 |
| 停用分类过滤 | 停用类的商品**不**进 `/api/home`、不进「为你推荐」 |
| 为你推荐排序 | 上架时间倒序、稳定不跳 |

**后端 `services/` 关键编排**:下单事务(成功落库;中途失败**整体回滚**,不留半张订单)。
**车机端**:ViewModel 状态流转、DTO→Model 映射、金额格式化(`¥%.2f`)用 JUnit 测。
工具:`pytest`(后端)/ `gradle test`(车机)。

---

## 2. 集成测试(Integration)· 接口 + 真数据库

每条用 Flask 测试客户端 + **临时 SQLite + seed**,跑完即销库。

**车机接口**:每个端点 ≥1 条
- `/api/home` 返 3~5 banner + 为你推荐(排除停用类)
- `/api/categories`、`/{id}/products`、`/products/{id}`(含已删商品 → 「已下架」)
- `POST /api/orders` 落库成功返 201;缺参/非法返错码
- `GET /api/orders` 按设备号过滤 + 倒序

**后台**:
- 登录:对 → 进;错 → 拦;**连错 5 次 → 限速锁**
- 分类 CRUD;**删有商品的分类 → 被拒提示**
- 商品 CRUD;**传图(multipart)→ 存 `uploads/` + 返回 URL 可访问**;删被订单引用的商品 → 允许且**老订单仍完整**
- 推荐位保存:**<3 或 >5 → 拒绝**;上移下移生效
- 订单倒序列表、仪表盘统计(商品数/订单数/营业额/最近 5 单)

---

## 3. 端到端(E2E)· 两种

**① API 级闭环(自动化,可进 CI)** —— 不需要模拟器,跑得快:
脚本按车机顺序串起来 `home → category → product → POST order` → `GET orders` 里出现该单 → 后台 `GET /admin/orders` 刷新也见到**同一单**。覆盖 B2(浏览购买)+ B3(订单闭环)。

**② 真机/模拟器手测清单(人工)** —— 在 1920×1080 横屏跑一遍:
- [ ] 验收 §3 happy path:推荐 → 分类 → 详情 → 下单 → 确认页 → QR → 点屏 → 支付成功 → 进订单页(该单可见)→ 点「推荐」回推荐页,**全程无报错**
- [ ] 验收 §5 断网即失败:拔网 → 全屏「网络异常」,不崩
- [ ] 验收 §1 公网登录页可达 + `admin / 见配置` 能登入
- [ ] 验收 §6 下线:`launchctl unload` 后公网 `curl` 返 404

---

## 4. 验收标准 ↔ 测试映射

| 需求共识 §5 验收 | 由哪层保证 |
|---|---|
| 1 公网可访问/登录 | 手测清单(部署后)+ 集成(登录页可达) |
| 2 后台 happy path | 集成(后台各操作)+ 手测 |
| 3 车机 happy path | 手测清单(模拟器)+ API 级 E2E(数据面) |
| 4 订单闭环 | API 级 E2E + 集成(下单→后台可见)+ 手测 |
| 5 断网即失败 | 集成(接口失败返错)+ 手测(真断网) |
| 6 下线开关 404 | 手测(`launchctl unload` → `curl` 404) |

---

## 5. 测试数据 / 怎么跑

- 每个集成/E2E 测试**独立建临时库 + seed,跑完销毁**,互不污染。
- 运行命令:`pytest`(后端)/ `gradle test`(车机)/ E2E 脚本 —— **具体命令待 `server/`、`android/` 落地后补实**。
- 「切片测试全绿」才算切片完成(CLAUDE.md 测试纪律)。

---

## 6. 明确不做(demo 范围)

| 不做 | 原因 |
|---|---|
| Compose UI 全自动化(模拟器录制回放) | ROI 低,用手测清单替代 |
| 性能 / 压力 / 并发测试 | demo,单设备演示 |
| 安全渗透测试 | 已知弱口令风险,靠「演示完下线」兜底(共识 §3) |
| 车机端网络层完整契约测试 | 接口少,集成测试已覆盖行为 |
