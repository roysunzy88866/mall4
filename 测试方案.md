# 测试方案 · RIDEMALL 车载商店

> 依据 [后端需求.md](后端需求.md)、[docs/architecture.md](docs/architecture.md)、[需求共识.md §5 验收标准](需求共识.md)。
> 三层:**单元 / 集成 / 端到端**。纪律:红绿循环(先写失败测试 → 转绿 → 重构),每写一个函数/接口立刻测(见 [CLAUDE.md](CLAUDE.md))。
> **现状(2026-06-04 实跑):后端 101 条全绿,`rules/` 覆盖率 100%**;车机端 26 条纯逻辑单测(JUnit)。

## 0. 策略一句话

后端是**数据正确性**的关键 → 自动化火力集中在后端(单元 + 集成);车机端 UI → 关键逻辑单测 + 一页**手测清单**走真机/模拟器。理由:Compose UI + 模拟器做全自动化录制回放,对这个 demo 性价比低,用手测清单替代更划算(这是「测试方式」的取舍,记录在案)。

---

## 1. 单元测试(Unit)· 跑得最快、最多

**后端 `rules/`(纯函数)—— 强制 100% 覆盖**,逐模块(均在 `server/tests/unit/`):

| 模块 | 测试文件 | 覆盖什么 |
|---|---|---|
| `rules/catalog` | `test_rules_catalog.py` | 为你推荐排序(上架倒序 + id 兜底)、停用分类过滤、描述节选截断、Banner 文案自动套 |
| `rules/ordering` | `test_rules_ordering.py` | 订单合计、下单入参校验(缺设备号/空商品/缺名/价非法/数量非法) |
| `rules/admin` | `test_rules_admin.py` | 登录限速(5 次/5 分,过期尝试剔除)、推荐位 3~5、图片扩展名白名单 |
| `rules/lifecycle` | `test_rules_lifecycle.py` | 阶段序、按时间推进、下一阶段、是否自动、物流节点、状态中文标签、可否取消/退货 |
| `rules/money` | `test_rules_money.py` | 元→分、分→元、HALF_UP 无浮点误差 |

**后端 `services/` 关键编排**:下单事务(成功落库;中途失败**整体回滚**,不留半张订单)——见集成测试 `test_api_orders.py::test_order_rollback_no_half_order`。

**车机端(`android/app/src/test/`,纯逻辑 JUnit)**:
- `CartTest` — 购物车合并、20 种上限、数量 1~99、选中合计、角标计数、仅取选中项快照
- `FormatTest` — 价格 `¥` 前缀、图片相对/绝对 URL 解析、空图返 null
- `MappersTest` — DTO→Model 映射(商品/Banner/分类/Home,含 null 处理)
- `OrderFormatTest` — 倒计时秒→`mm:ss`、状态码→中文
- `CarouselTest` — 轮播自增回卷、单/空安全、夹取范围
- `DeviceIdTest` — 设备号缺失/空白时生成、存在则复用

工具:`pytest`(后端)/ `gradle test`(车机)。

---

## 2. 集成测试(Integration)· 接口 + 真数据库

每条用 Flask 测试客户端 + **临时 SQLite + seed**,跑完即销库(均在 `server/tests/integration/`)。

**车机接口**
- `test_api_catalog.py` — `/api/home`(banner + 为你推荐,排除停用类/停用类商品不进 banner、空态)、`/categories`(启用按 sort_order)、`/{id}/products`、`/products/{id}`(含 404 已下架)
- `test_api_detail_images.py` — 商品图集按 sort_order 排序
- `test_api_orders.py` — `POST /orders`(成功 201 / 缺设备号 / 空商品)、`GET /orders`(按设备号倒序、仅本机、空态)、整单快照不受商品改价影响、浏览→下单可见、落库失败不留半单
- `test_api_lifecycle.py` — 下单初始 `paid` 带物流、详情带物流、详情 404、按时间自动推进到运输中/已签收、后台接管后停自动
- `test_api_returns.py` — 在途可取消 / 已签收拒取消 / 非本设备拒、窗口内可退货 / 超窗拒 / 须填原因、后台通过/拒绝退货、营业额扣除已取消+已退款、退货页可加载、后台快进退货窗、非审核态/重复审核为幂等空操作

**后台**
- `test_admin_auth_dashboard.py` — 登录对→进/错→拦/**连错 5 次→限速**、未登录重定向、仪表盘统计
- `test_admin_categories.py` — 分类 CRUD + 排序;**删有商品的分类→被拒**
- `test_admin_products.py` — 商品 CRUD;**价格→分**、**传图(multipart)→存 uploads**、坏图/坏价拒绝、**删被订单引用商品→老订单快照仍完整**
- `test_admin_banners.py` — seed 4 个推荐位、加/超 5 拒/去重、**移出至少留 3**、上下移
- `test_admin_orders.py` — 订单列表(seed/新单)、按状态过滤、详情带物流、后台改状态接管、快进一步、一键直达签收、仪表盘状态分布中文

---

## 3. 端到端(E2E)· 现状与口径

> ⚠️ **无独立 E2E 自动化脚本**(早期方案曾设想一个 `home→order→后台同单` 的 CI 脚本,实际未单独建)。
> 该闭环目前由**集成测试分段覆盖**,已满足验收数据面:
> - `test_api_orders.py::test_e2e_browse_then_order_appears` —— 浏览 → 下单 → `GET /orders` 见到该单(车机侧闭环);
> - `test_admin_orders.py::test_orders_page_shows_newly_placed_order` —— 新下的单在后台订单页可见(后台侧闭环)。

**真机/模拟器手测清单(人工)** —— 在 1920×1080 横屏跑一遍。各 change 已各自留有 `openspec/changes/archive/<change>/manual-test.md`,验收时按下表逐条核:
- [ ] §3 happy path:推荐 → 分类 → 详情 → 加购/下单 → 确认页 → QR → 点屏 → 支付成功 → 进订单页(该单可见)→ 看物流时间线,**全程无报错**
- [ ] 取消/退货:在途单可取消;签收单 7 天内可申请退货 → 后台审核通过/拒绝,状态与物流随之变
- [ ] §5 断网即失败:拔网 → 全屏「网络异常」,不崩
- [ ] §1 公网登录页可达 + `admin / 见配置` 能登入
- [ ] §6 下线:`launchctl unload` 后公网 `curl` 返 404

---

## 4. 验收标准 ↔ 测试映射

| 需求共识 §5 验收 | 由哪层保证 |
|---|---|
| 1 公网可访问/登录 | 手测(部署后)+ 集成(登录页可达/限速) |
| 2 后台 happy path | 集成(分类/商品/推荐位/订单/退货各操作)+ 手测 |
| 3 车机 happy path | 手测(模拟器)+ 集成(浏览→下单数据面) |
| 4 订单闭环 + 生命周期 | 集成(下单→后台可见 + 状态机/物流/取消/退货)+ 手测 |
| 5 断网即失败 | 集成(接口失败返错)+ 手测(真断网) |
| 6 下线开关 404 | 手测(`launchctl unload` → `curl` 404) |

---

## 5. 测试数据 / 怎么跑

- 每个集成测试**独立建临时库 + seed,跑完销毁**,互不污染(见 `server/tests/conftest.py`)。
- 运行命令:
  - 后端:`.venv/bin/python -m pytest`(含 `rules` 100% 覆盖率闸门,见 `pyproject.toml` / `.pre-commit-config.yaml`)。
  - 车机:`cd android && JAVA_HOME=$(/usr/libexec/java_home -v 17) ./gradlew :app:testDebugUnitTest`。
- 「切片测试全绿 + `rules` 100% 覆盖」才算切片完成(CLAUDE.md 测试纪律 / `scripts/archive` 归档闸门)。

---

## 6. 明确不做(demo 范围)

| 不做 | 原因 |
|---|---|
| Compose UI 全自动化(模拟器录制回放) | ROI 低,用手测清单替代 |
| 性能 / 压力 / 并发测试 | demo,单设备演示 |
| 安全渗透测试 | 已知弱口令风险,靠「演示完下线」兜底(共识 §3) |
| 车机端网络层完整契约测试 | 接口少,集成测试已覆盖后端行为 |
