# RIDEMALL 后端需求 · 共创草案

> **状态**:✅ 已确认(2026-06-03 经「需求共创」逐条拍板;D1~D6 用户拍板,D7~D9 默认获认可)
> **依据**:建立在 [需求共识.md](需求共识.md)(范围合同,已锁定)+ `design_handoff_ridemall_carstore/`(高保真设计稿)之上。本文只补**后端(服务器那一侧)**,不重开已锁定的前端范围。
> **reasoning 图例**:🔒 你已拍板 · 📌 我做的合理默认(你可否决)· 💡 我的假设(未确认)· ❓ 待定
> **货币/快照/锚点等硬事实**:遵循 `需求共识.md §3` 锚点表,不在此重抄数值。

---

## 0. 一句话

服务器要做三件事:① 存好分类 / 商品 / 图 / 推荐位 / 订单;② 给车机端提供「拉数据 + 下单」的接口;③ 给管理员提供一个网页后台维护这些东西。技术栈(Flask + SQLite,见 [需求共识.md §3](需求共识.md))已锁。

---

## 1. 后端要存什么(数据模型 · 人话)

服务器里开 7 个「册子」(数据表):

```
分类 ──┬──< 商品 ──┬──< 商品图(1 主图 + 最多 3 细节,共最多 4 张)
       │           └── 被 推荐位(3~5 个)引用
       └ 停用 → 整类在车机端隐藏(导航 + 为你推荐都不含)

订单 ──< 订单行(整单快照:商品名/图/价,下单那刻定格,之后改删都不影响)
    └ 按「设备号」归属;后台看全部,车机只看本机这一台的
```

| 册子 | 存什么 | 关键点 |
|---|---|---|
| **分类** | 名称、排序、是否停用 | 停用 🔒=整类在车机藏起来;删除才是真抹掉 |
| **商品** | 名称、价格、描述、库存、所属分类(一个商品只属一类)、上架序 | 库存 📌 仅展示,不扣不拦(共识已定) |
| **商品图** | 所属商品、图 URL、顺序 | 📌 第 1 张=主图(也用作该商品当 Banner 时的图);最多 4 张(1 主 + 3 细节,设计稿 🔒) |
| **推荐位** | 引用哪个商品、顺序 | 文案 🔒 自动套商品信息;数量 🔒 强制 3~5(共识 F4a) |
| **订单** | 设备号、下单时间、状态(恒「已支付」)、合计金额 | 状态 📌 只有「已支付」(无真支付,共识 §2) |
| **订单行** | 所属订单、商品名快照、商品图快照、价格快照、数量(恒 1) | 🔒 整单快照(你拍的);价 🔒=进确认页锁定价(共识 A8) |
| **登录限速** | IP、失败时间戳 | 📌 5 次/5 分钟/IP,滑动窗口 5 分钟后自动解(共识) |

> 收货信息 📌:固定假数据(张三/138****8888/上海…),前端写死展示,后端不单独管理。

---

## 2. 车机端会向服务器要什么(对外接口 · 人话)

车机端每次请求都带「本机设备号」📌(虚拟生成,见共识 A9)。一共 5 类:

1. **拉推荐页**:一次拿到「轮播 3~5 张(每张:商品名/描述节选/价/主图)」+「为你推荐网格(=全部上架商品,排除停用分类下的)」📌 合并成一个接口,省一次来回。
2. **拉某分类商品**:点左侧某分类 → 拿该类全部商品。
3. **拉商品详情**:名/价/描述/图集(标签「7天无理由·包邮·自营」是前端写死,后端不管)。
4. **下单**:🔒 车机把「整单快照(商品名/图 + 进确认页锁定的价)」发给服务器 → 服务器**先落库成功,才回成功** → 车机收到成功才显示「支付成功」;失败就弹「网络异常」、不算成功。
5. **拉本机订单历史**:🔒 按本机设备号拉,倒序;只有服务器一个真相源(车机不本地存订单)。

> 车机导航 📌:`推荐`(固定首)+ 后台**启用的分类**(按排序,所以后台增删分类车机会跟着变)+ `订单`(固定末)。这正是共识「分类即导航」的后端含义。

---

## 3. 后台管理员能做什么(每个操作背后)

网页后台(服务端渲染),登录后:

| 模块 | 操作 | 后端行为 |
|---|---|---|
| 登录 | 账号密码(配置项,不硬编码)📌 | 失败 5 次/5 分钟/IP 限速 🔒 |
| 仪表盘 | 看 商品数 / 订单数 / 营业额 / 最近 5 单 | 营业额 📌=所有已支付订单合计之和 |
| 分类 | 增 / 删 / 改名 / 改排序 / 停用 | 删有商品的分类 📌 拦截,提示先清空(避免孤儿商品);停用 🔒 整类在车机隐藏 |
| 商品 | 增 / 删 / 改 / 传图 / 选分类 / 定价/描述/库存 | 删被订单引用的商品 📌 允许(订单是整单快照,不依赖商品行) |
| 推荐位 | 从所有商品挑 3~5 个 / 上移下移排序 | 保存时校验 3~5,越界报错 🔒;被选商品若被删/停用 📌 自动移出推荐位,若不足 3 提示补 |
| 订单 | 倒序列表:时间/商品/价/设备号 | 🔒 手动 F5,不自动刷新(共识 A7) |

---

## 4. 关键业务规则(已拍板的硬规则)

- 🔒 **整单快照**:下单那刻把商品名、图、价存进订单行,管理员之后改/删商品都不影响历史订单显示。
- 🔒 **价格快照来源**:订单价 = 车机在「进确认页」锁定、随下单上送的价(共识 A8);后端**原样存,不在支付时重取现价**。本 demo 信任车机上送价(无真支付,可接受)📌。
- 🔒 **推荐位 3~5**:后台强制下限 3 上限 5;seed 默认 4 张(共识 F4a)。
- 🔒 **停用 vs 删除**:停用=车机整类隐藏、数据保留可重启用;删除=真抹掉(有商品先拦)。
- 🔒 **订单只存服务器**:车机按设备号拉,后台看全部;车机不本地存订单。
- 🔒 **先落库再成功**:下单先落库成功才显示支付成功,保证「凡显示成功后台必看得到」。
- 📌 **为你推荐排序**:按上架时间倒序(新品在前),稳定不跳屏(不用随机,避免每次刷新乱跳)。
- 📌 **库存**:仅展示,不扣减、不拦截下单。
- 📌 **设备号**:车机首次启动随机生成、存本地,每次请求带上;后台订单显示它。

---

## 5. 启动预置数据(seed · 首次启动库空时)

- 🔒 预置:5 分类 + ~18 商品(用设计稿 `data.js` 数值)+ 4 推荐位 + **3~5 笔历史订单**(挂在一个「演示设备号」上,让后台仪表盘一开就有数据)。
- 🔒 车机首开订单页**空态**:因为车机用自己随机生成的设备号,拉不到那几笔演示订单;现场买一单即出现。
- 📌 商品图:先用占位图/免费图顶上;真实图素材等你提供(共识 §6 待补,不阻塞)。

---

## 6. 图片存储 📌(共识 §3 已定)

上传的商品图存服务器本地 `server/uploads/`,文件名用随机串避免冲突,通过 `/uploads/<文件>` 这样的 URL 提供给车机和后台。

---

## 7. 登录与安全 🔒(共识 §3)

- 单管理员,账号口令为配置项(不写死在代码里)。
- 登录失败 5 次/5 分钟/IP 限速兜底;不上 Cloudflare Access。
- 已知风险:公网弱口令 → **演示完必须 `launchctl unload` 下线**闭合。

---

## 8. 错误 / 断网 / 边界语义

- 🔒 车机任何接口失败/断网 → 前端全屏「网络异常,点击重试」;**不做本地缓存**(订单也走服务器,所以断网时订单页一样失败)。
- 🔒 下单 POST 失败 → 车机不显示支付成功,弹网络异常。
- 📌 删有商品的分类 → 后端拒绝并提示「请先清空该分类」。
- 🔒 推荐位保存数量 <3 或 >5 → 拒绝 + 提示。
- 📌 商品必填/价格非法 → 后端校验,失败回提示。
- 📌 车机请求一个已删商品的详情 → 返回「商品已下架」(车机优雅处理)。

---

## 9. 决策日志(谁拍的 · reasoning path)

| # | 决策 | 结论 | 谁拍 |
|---|---|---|---|
| D1 | 推荐位文案来源 | 自动套商品信息(后台只选商品+排序) | 🔒 用户 2026-06-03 |
| D2 | 订单是否整单快照 | 整单快照(名/图/价都存) | 🔒 用户 2026-06-03 |
| D3 | 车机订单历史数据源 | 只存服务器,按设备号拉 | 🔒 用户 2026-06-03 |
| D4 | 支付落库时机 | 先落库成功才显示支付成功 | 🔒 用户 2026-06-03 |
| D5 | 停用分类语义 | 整类在车机隐藏(导航+为你推荐) | 🔒 用户 2026-06-03 |
| D6 | 是否预置订单 | 预置 3~5 笔(后台有数据),车机空态起步 | 🔒 用户 2026-06-03 |
| D7 | 为你推荐排序 | 上架时间倒序,稳定不跳 | 📌 默认·用户已认可 |
| D8 | 删被订单引用的商品 | 允许(整单快照不依赖商品) | 📌 默认·用户已认可 |
| D9 | 删有商品的分类 | 拦截,提示先清空 | 📌 默认·用户已认可 |

---

## 10. ❓ 待定 / 待你定

1. ✅ **本文档归属** → 独立 `后端需求.md`(2026-06-03 用户定);`需求共识.md` 顶部已加指针指来。
2. ✅ **商品图素材** → 先用占位/免费图起步(2026-06-03 用户定);真实图你以后随时给,直接换 seed。
3. 共识 §7 灰色项(导出订单 Excel、二级子分类、操作日志、数据库备份等)— 后端默认都不做,你要再提。

---

## 11. 与 需求共识.md 的衔接

✅ 已同步:共识 §1「车机端不存任何数据(订单历史除外…)」一句已按 **D3** 收紧为「车机端不本地存业务数据,订单也只存服务器、按设备号拉」(2026-06-03 用户认可)。语义未变,仅澄清不本地缓存。
